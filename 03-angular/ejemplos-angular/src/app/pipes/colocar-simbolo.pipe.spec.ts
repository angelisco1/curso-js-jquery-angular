import { ColocarSimboloPipe } from './colocar-simbolo.pipe';

describe('ColocarSimboloPipe', () => {
  it('create an instance', () => {
    const pipe = new ColocarSimboloPipe();
    expect(pipe).toBeTruthy();
  });
});
